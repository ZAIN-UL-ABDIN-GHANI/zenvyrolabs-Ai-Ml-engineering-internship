AI COMPONENTS

AI-related files
- services/aiService.py
- app.py
- README.md
- frontend/index.html
- frontend/demo.html

Prompt templates
- Not found in the project.
- The code contains comments that describe the intended system prompt behavior, but no completed prompt template is implemented.

Local LLM configuration
- services/aiService.py sets self.ollama_url to http://localhost:11434/api/generate.
- services/aiService.py sets self.model to mistral.
- README.md states that a local LLM runner such as Ollama, LM Studio, or GPT4All should be used.

Conversation handling
- DebateConductor defines self.debate_history as an empty list.
- No code in the repository currently appends to or reads from debate_history in a completed implementation.
- app.py exposes a next-turn route intended to pass prior debate context to the AI layer.

Memory handling
- The only explicit memory-related structure in the code is debate_history in services/aiService.py.
- No persistent memory store, database, or external memory file was found.

AI services
- DebateConductor is the AI service class.
- generate_agent_a_response(topic) is intended to produce the advocate response.
- generate_agent_b_response(topic) is intended to produce the challenger response.
- Both generation methods currently return placeholder strings.

Model configuration
- The active model name configured in code is mistral.
- No additional AI model configuration file was found.
- No prompt library, persona configuration file, or agent registry file was found.
