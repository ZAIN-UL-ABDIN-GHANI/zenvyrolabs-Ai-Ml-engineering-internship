MACHINE LEARNING COMPONENTS

Dataset files
- historical_debates.csv is referenced in app.py and README.md.
- The dataset file historical_debates.csv was not found in the repository.

ML models
- services/mlJudge.py defines DebateRegressionJudge.
- The code comments reference RandomForestRegressor and LinearRegression as intended model options.
- No trained model artifact, pickle file, or saved estimator file was found.

Feature extraction
- extract_NLP_features(text) is defined in services/mlJudge.py.
- The current returned features are word_count, complexity_score, and sentiment.
- complexity_score and sentiment are present as placeholders in the current code.
- The code comments mention additional possible features such as vocabulary richness, average sentence length, exclamation count, and VADER sentiment.

Training code
- train_model(dataset_path) is defined in services/mlJudge.py.
- The current code prints dataset-loading and training messages, but the implementation body is marked as pending intern code.
- The method returns a placeholder metrics dictionary with mse and r2_score values.
- app.py calls train_model("historical_debates.csv") from the training route.

Prediction code
- predict_score(text) is defined in services/mlJudge.py.
- The method raises an exception if self.model is None.
- The method currently returns a placeholder score of 8.5 after the guard.

Scikit-Learn usage
- Scikit-Learn is referenced in README.md and in comments inside services/mlJudge.py and frontend/demo.html.
- No scikit-learn import statement is present in the current Python code.
- No active scikit-learn pipeline is implemented in the repository files.
